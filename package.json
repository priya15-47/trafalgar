{
  "name": "trafalgar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tw": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch",

    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.7"
  }
}
